server {
    listen 80;
    server_name yourdomain.com;

    root /var/www/company-website/client/dist;
    index index.html;

    location /api/ {
        proxy_pass http://localhost:5000/;
    }

    location / {
        try_files $uri /index.html;
    }
}
